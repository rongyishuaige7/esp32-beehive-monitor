#ifndef WIFI_CREDENTIALS_EXAMPLE_H
#define WIFI_CREDENTIALS_EXAMPLE_H

// Copy this file to wifi_credentials.h and edit that local, Git-ignored copy.
// The public project intentionally ships without a default network or password.
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

#endif
